# Next Milestone Browser Acceptance Evidence

## Production route recovery

The latest READY production deployment was opened at `https://senota-3gi0g6rmk-senota-s-projects.vercel.app/`. Selecting the Luna Brain navigation item successfully rendered `/luna/brain`; the prior blank/intercepted navigation was **not reproduced** in this browser run.

## Visible verified state

The deployed Luna Brain workspace showed the Three.js canvas, a status of `Luna's brain online · 283 anatomical meshes`, the Navigator with `283 structures`, the Anatomical Inspector, and the Nanobot System. The primary menus (`Anatomy`, `Inspector`, `Nanobots`, `Luna`, `Scales`, `Missions`, and `View`) were present, as were minimize/close controls for all three panels. The unselected Macro observation state was visible, along with the expected Macro target prompt and disabled actions prior to selection.

This confirms that the production brain route now renders its primary workspace shell. Structure selection, Inspector scientific evidence, Nanobot target-kind display, mission execution, panel restoration, and lower-scale gate behavior remain to be checked in the same live session.

## Follow-up interaction limitation

Attempting to expand the Navigator’s left-hemisphere branch caused the browser automation surface to transition to `about:blank` after an initially successful Luna Brain render. The prior production route blank-transition issue therefore remains reproducible during deeper browser interaction, even though the initial `/luna/brain` workspace rendered successfully. The browser run cannot yet claim acceptance for a selected structure, scientific Inspector card, Nanobot target display, or mission control interaction.

## Fresh route-level reproduction

A direct browser navigation to the same latest READY production `/luna/brain` URL transitioned to `about:blank` before an interactive page snapshot became available. The browser console contained no output in the immediately preceding console capture. This reproduced the failure without a successful in-app Navigator event in this particular attempt, so the current evidence does **not** establish that the Navigator itself mutates application navigation. It is consistent with a browser automation or route-loading interaction issue, but requires local reproduction and source audit before a conclusion.

## Local reproduction

A temporary local development instance on `http://localhost:3000` was started and `/luna/brain` was opened through the same browser automation surface. The page likewise rendered a blank white view with no interactive elements. The local console contained only the React DevTools informational message and no application exception. This reproduces the automation-visible blank behavior outside Vercel, so the available evidence rules out a Vercel-only rewrite/deployment interception and shows no console-visible React failure. The Navigator source uses a semantic `button type="button"` and invokes only structure selection/local expansion state; it has no anchor, href, location, or router mutation.

## Local client-side navigation reproduction

The local landing page rendered normally and the browser automation selected its Luna Brain button. The URL changed to `http://localhost:3000/luna/brain`, but the automation returned no page elements or screenshot immediately afterwards. This reproduces the issue through client-side navigation as well as direct-route loading. The prior route/source audit still finds no Navigator anchor, href, `window.location`, or router mutation. The failure therefore cannot currently be attributed to Vercel, an explicit Navigator navigation handler, or a console-visible client exception.

## Repeated production landing-to-Luna transition

A fresh production landing page rendered normally. Selecting its Luna Brain button changed the URL to `/luna/brain`, but the automation response again contained no rendered elements or screenshot. This reproduces the route-transition symptom from a known-good production landing state.

## Independent headless limitation

A standalone Chromium headless request to the production URL received a large Vercel control-plane/protection document rather than the SenotaAI application root. This independent request did not share the authenticated interactive browser session, so it cannot validate the application route or be used as a substitute for production acceptance. It does not indicate an app source failure; it records an access-control difference between the two browser contexts.

## Guarded-renderer release retest

Deployment `dpl_2NA5MgqrgM7TK8pPpcMfezkQ2fiP` for commit `39e2e96` was READY. Its landing page rendered normally. Selecting Luna Brain again changed the URL to `/luna/brain`, then the browser DOM probe reported `href: about:blank`, no title, no `#root`, and zero canvases. Because the React root was absent, the new guarded WebGL fallback could not have rendered; the transition occurs before the page application is available to the browser automation session. This supports the conclusion that the failure is not an in-app Navigator handler, a Three.js renderer exception, or a Vercel-only application build path.

## Review-indicator deployment retest

Deployment `dpl_FRfkPPvZQ1Z4aKft2Ek9ahqsXUmj` for commit `ca7b7b4` was READY. Its landing page rendered normally. Selecting Luna Brain again changed the URL to `/luna/brain`, after which the browser automation reported no elements or screenshot. The pre-existing automation route-transition blocker remains; visual acceptance of the red Navigator indicator cannot be claimed from this session.

## Scientific Review Center deployment observation

Deployment `dpl_EqS7ebanAEneAAQkLUxm9tN2rL3s` successfully rendered `/luna/brain` in the browser automation session, including canvas, Navigator, Inspector, Nanobot panel, workspace controls, and the new Scientific Review entry. The entry displayed `COMPLETE` and `0 of 0 reviewed` rather than the expected review counts. This shows the route-level browser issue did not reproduce on this deployment, while identifying a separate review-record loading defect that must be diagnosed before review acceptance can pass.

The rendered deployment’s `/api/brain-science/canonical-identities` request returned HTTP 200 and a non-empty identity payload beginning with canonical source records. Before a post-fetch UI inspection could complete, the browser automation session transitioned to `about:blank`. Thus the initial `0 of 0` presentation may have been the transient pre-fetch state, but visual confirmation of the loaded 102-count badge is blocked by the recurring automation-session transition.

## Final review-loading-state production retest

For release `f524664`, production deployment `dpl_CkMBZV2juvUxwtvLz7pysftygBMb` mounted `/luna/brain` successfully through the normal landing-page navigation. The canvas, 283-mesh Navigator, Inspector, Nanobot panel, and one-click Scientific Review control rendered. Before the asynchronous canonical-identity request settled, the button correctly displayed `…` with accessible text `Scientific review records loading`; it did not report the misleading prior `Complete 0 of 0` state.

A post-fetch browser view did not expose page elements; a document probe then found `href: about:blank`, no React root, and no canvas. The repeating automation-session transition therefore again prevented validation of the settled 102-record badge, Review Center controls, live Navigator updates, or reload persistence in production. This is a browser acceptance blocker, not evidence of a source-data or review-state failure.

Release `ee50894` (`dpl_GyAjmSBPZGRqKLggrmMoeQg8aoCy`) repeated the successful normal landing-to-Luna transition. It rendered the Three.js canvas, workspace, Navigator shell, Inspector, Nanobot panel, and the Scientific Review control with `…`/`Scientific review records loading`; no false completion presentation appeared. A bounded post-fetch DOM query found the React root and one canvas still mounted at `/luna/brain`; the button’s accessible label was `Scientific Review: 102 structures require review` and its visible content was `Scientific Review 102 — 0 of 102 reviewed`. This verifies that the prior `0 of 0` state was transient asynchronous loading, not a persistent registry or endpoint failure.

When the automation then attempted to open the populated review control, the browser document had already transitioned to `about:blank`; the control no longer existed in the DOM. This retains a limited live-browser acceptance blocker for entering the detailed panel and safely exercising approval, rejection, Navigator updates, and reload persistence. No review decision was submitted during validation.

## Navigation trace attempt

A fresh production landing document received a passive trace for unload/page lifecycle, client exceptions, unhandled rejections, history changes, document anchor/button clicks, and `window.open` calls. The normal Luna Brain sidebar control was then selected. The reported URL changed to `/luna/brain`, but the automation immediately exposed zero interactive elements and no screenshot. A console probe showed the active document was a new `about:blank` navigation with `redirectCount: 0`, a null origin, no React root, and no permitted local storage access. Navigating back to the same production origin immediately restored the normal landing document. The persisted trace contained exactly three entries: installation, the `Luna Brain` button click, and Wouter’s expected `history.pushState("/luna/brain")`. It contained no application unload/pagehide, redirect, window-open, client error, or unhandled rejection. The subsequent console log also contained no client error. Together with Vercel’s zero runtime errors and successful 200 responses for the Luna/scientific endpoints, this is conclusive evidence that the production application does not navigate the document to `about:blank`; the browser-automation context is being externally replaced after the legitimate SPA route change.

A second controlled run used `history.pushState("/luna/brain")` and a synthetic `popstate` from a stable production landing document inside one browser execution. After 1.8 seconds, the actual Luna React root and one canvas were mounted at `/luna/brain`, with 78 controls and the review badge in its correct loading state. A subsequent passive wait/query found a newly navigated `about:blank` document. Because the page was demonstrably mounted after the route change and later disappeared between automation invocations, this further rules out a Luna click handler, router target, Vercel redirect, or immediate render exception as the initiator.

The same browser tooling also rendered a visually blank local landing page at `http://localhost:3000/` and exposed zero interactive elements. A console query simultaneously found the correct local URL, a present React root containing 34,764 bytes of rendered DOM and the normal landing-page text, with a normal zero-redirect navigation entry. This independent local reproduction proves that the blank presentation can occur while the application remains mounted and is unrelated to production deployment, Vercel routing, or an application navigation.

A bounded local Review Center test attempted to route, wait for 102 loaded records, open the center, and select a review record. It stopped before an approval/rejection control was ready and did not write a review overlay. The next console query was denied because the browser document had again become `about:blank`. This preserves the test dataset and confirms the automation replacement also interrupts local, not only production, interactions.

## Isolated local workflow acceptance

A DevTools Protocol harness created a disposable local browser context and ran the complete review workflow against `http://localhost:3000`. It entered Luna through the normal landing control, confirmed the canvas, Navigator, Inspector, Nanobot panel, one-click Scientific Review Center, and the settled `102` review count. It exercised the `97` high-confidence, `5` ambiguous, and `181` unmapped filters; unmapped rows were verified neutral.

The harness then approved one source-backed record and confirmed the live badge changed from `102` to `101`, its Navigator state changed from `REQUIRES_REVIEW` to `APPROVED`, and the Inspector displayed `SCIENTIFIC IDENTITY APPROVED` while retaining the explicit no-Luna-to-MNI, no-authoritative-Julich-crosswalk, and coordinate-unavailable limitations. It opened a fresh Luna page in the same disposable browser context and confirmed the approval persisted. It attempted a rejection without a reason and confirmed validation blocked persistence, then rejected a second record with an explicit test-only reason. The live badge became `100`, the Navigator state became `REJECTED`, and the Inspector displayed `SCIENTIFIC IDENTITY REJECTED` with the same spatial and mission limitations. The rejection persisted on a fresh page. `Keep for review` removed the rejection overlay and restored the count to `101`; it then removed the temporary approval and restored the count to `102`.

The harness compared the immutable HRA/UBERON/version/source URL/evidence fields of the two test records before and after the decisions, with no difference. It explicitly removed the temporary local overlay before disposing its browser context; no reviewer decision was left behind. The context retained the Luna route and canvas throughout, demonstrating no application-driven `about:blank` navigation. Chromium emitted only the environment-level software-WebGL deprecation warning and one source-less `SyntaxError: Unexpected token '<'` parser event; the workflow itself completed with no failed assertions. The parser event remains under investigation and is not associated with a failed API, source-evidence mutation, or Luna route transition.

The repeated visual automation session transition to `about:blank` was independently reproduced with a populated React root both locally and on Vercel. Passive page instrumentation recorded only the expected Wouter `history.pushState("/luna/brain")` and no unload, redirect, window-open, error, or rejection. The isolated DevTools context completed the entire route and review workflow without `about:blank`. The evidence therefore attributes the blank-document issue to the screenshot/browser automation surface rather than the SenotaAI route, BrainViewer, Vercel deployment, or review code.

## Release validation note

The final full test run reported 208 passing tests and 2 skipped tests. It also reported 14 expected pre-existing failures across 12 Supabase nanite/NPC administrator suites because the sandbox lacks the required Supabase nanite configuration and administrator session. The Scientific Review Center and scientific/nanobot safety suites pass independently.

## Production workflow acceptance — commit `61e2a3a`

Deployment `dpl_53hNCboDWLX8mpUnBZTGX7mJXiAU` reached Vercel Ready at `https://senota-bfehhe25f-senota-s-projects.vercel.app` for commit `61e2a3a`. The guarded Chrome DevTools Protocol acceptance harness ran the complete workflow on that deployed production application in the authenticated browser profile. It first required an empty local review overlay and installed exact restoration in a `finally` path. It created temporary local test decisions only after that protection was established and removed them before the test page closed.

Production acceptance passed all assertions. The normal landing control opened `/luna/brain`; canvas, Navigator, Inspector, Nanobot panel, and the Scientific Review entry were present. The live badge settled at `102` remaining. The Review Center opened in one action and passed the `97` high-confidence, `5` ambiguous, and `181` unmapped filters; unmapped rows were neutral. A source-backed record began `REQUIRES_REVIEW`, was approved, changed the live badge to `101`, changed the Navigator to `APPROVED`, and showed `SCIENTIFIC IDENTITY APPROVED` in the Inspector while retaining no Luna-to-MNI, no authoritative Julich crosswalk, and coordinate-unavailable language. The approved local decision persisted on a fresh deployed Luna page.

A second record rejected without a reason was blocked. It was then rejected with the explicit temporary reason `Isolated acceptance test rejection; source evidence unchanged.`, changing the badge to `100`, the Navigator to `REJECTED`, and the Inspector to `SCIENTIFIC IDENTITY REJECTED`; this too persisted on a fresh deployed page. `Keep for review` removed the rejection and restored `101`; clearing the temporary approval restored `102`. Before/after comparisons of HRA entity, UBERON identifier, source version, source URL, and evidence were identical. The production overlay was finally absent, so no test reviewer decision remains. The final page remained a mounted Luna route with a canvas; there was no `about:blank` navigation. No script request was served HTML and no application runtime error occurred. The only browser messages were Chromium’s known software-WebGL deprecation warnings in the test environment.


## 2026-08-26 — Approved Julich context production acceptance (`7fa3b8e`)

The ready production deployment `https://senota-9ziso4sdq-senota-s-projects.vercel.app` was validated with a non-mutating Chrome DevTools workflow after the normal landing-page **Luna Brain** action. The app mounted the `/luna/brain` route and canvas; the Scientific Review control settled as `Scientific Review complete: 102 approved identities`; and the Review Center displayed `102 / 102`, `97` high-confidence records, and `5` ambiguous records.

The workflow selected `Allen_hypothalamus_L` and confirmed the source API state `approved`, review date `2026-08-26`, and method `user-attested-approved-review-population`. The Navigator reported `APPROVED`. The Inspector displayed the Julich-Brain v3.1 context with `UNMAPPED` classification and the unchanged `Luna → MNI: Not established` limitation. The direct form accepted independently entered `0,0,0` MNI ICBM 152 2009c millimetre values only; it did not receive a Luna/viewer/GLB coordinate.

The released structure API returned `canonicalStatus: APPROVED`, `julichStatus: UNMAPPED`, `julichRegionId: null`, `coordinate: null`, and `lunaToMni: not-established`. The mapping summary returned exactly `102` approved records with `0` authoritative, `0` probabilistic, `0` domain-review, and `102` unmapped Julich relations. The browser session reported no runtime exceptions. No reviewer decision, source identity, provider mapping, coordinate registration, or nanobot capability was written or changed during this acceptance test.

The prior screenshot-automation surface still transitioned to `about:blank` after a conventional route click. The production acceptance above uses the independent DevTools path that previously established this transition is external to the application route; the normal in-app navigation and deployed workflow remained mounted and completed successfully.

## 2026-08-26 — P33 registration-quality production acceptance (`2ef803e`)

The READY production deployment `dpl_4SDfu9n9ewQJgfaepdjpKGWULe8f` at `https://senota-o2bednx8k-senota-s-projects.vercel.app` was validated with the non-mutating DevTools harness `scripts/runP33RegistrationBrowserAcceptance.mjs`. Normal landing-page navigation opened `/luna/brain`; the Three.js canvas, Anatomical Navigator, P33 Inspector card, and Nanobot panel were present, and the run reported no runtime exception.

The deployed `/api/brain-science/manifest` preserved the immutable source SHA-256 `c5711a1a8bc62ca930b8bcf076def15315c11f5ad9bc7901e51f698406d38dbc`, exact target `ebrains-mni-icbm-152-2009c` / `2009c nonlinear asymmetric`, legacy availability `unavailable`, P33 quality status `NOT_ESTABLISHED`, `transformEnabled: false`, and `transformArtifact: null`. Both explicit raw-GLB↔MNI directional records remained `unavailable`. The registration endpoint independently returned `not-established` with the same quality status.

The Inspector visibly displayed the P33 `NOT ESTABLISHED` registration disclosure and the statement that no Luna-native or reverse-MNI coordinate is emitted. The approved scientific-review population remained intact (`Scientific Review complete: 102 approved identities`); `Allen_hypothalamus_L` remained `APPROVED` in the Navigator. The structure API still returned `coordinate: null` and `lunaToMni: not-established`; Julich mapping remained exactly 0 authoritative, 0 probabilistic, 0 requires-domain-review, and 102 unmapped. A Tissue observation returned `coordinate: null` and `operationEnabled: false`, preserving the Macro-only lower-scale gate. The direct MNI→Julich form was rendered and accepted only explicit independently entered MNI 2009c millimetre values. No provider mapping, coordinate, reviewer decision, source identity, or nanobot capability was mutated during acceptance.
