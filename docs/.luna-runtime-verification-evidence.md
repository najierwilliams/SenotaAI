# Luna Durable Runtime Verification Evidence

## 2026-08-27 Vercel project state

The connected Vercel deployment context returned the existing linked project `senota-ai` (`prj_z3LujnP12wS8jjfH2E2Cm7SrqvbC`) under team `SENOTA's projects` (`team_edfdjmh2fAVvOzNF9PQGoCcv`) on the `hobby` plan. This is an observed project property, not an inference.

## Official Vercel Queue findings

The [Vercel Queues JavaScript SDK documentation](https://vercel.com/docs/queues/sdk) states that the TypeScript SDK is installed as `@vercel/queue`; producers call `send(topic, payload, options)` and receive a `messageId`; a Node/Express consumer can be built with `QueueClient.handleNodeCallback`; and a push consumer requires an `experimentalTriggers` entry in `vercel.json` using `type: queue/v2beta` and a topic.

The [Vercel Queues concepts documentation](https://vercel.com/docs/queues/concepts) describes durable, at-least-once delivery with retries and visibility timeouts. It says JavaScript/TypeScript push consumers are configured in `vercel.json`, are invoked by Vercel's private queue infrastructure rather than a public route, and require idempotent handling because delivery can occur more than once.

## Official Vercel Workflow findings

The [Vercel Workflows documentation](https://vercel.com/docs/workflows) describes Workflows as a managed durable system built on the `workflow` SDK. It requires a workflow implementation, uses durable workflow runs/steps, and stores event/state information managed by Vercel. The documentation presents `start()` as returning a workflow run when a configured workflow is started.

## Initial repository consequence

At the start of this audit, the repository contained neither `@vercel/queue` nor `workflow`; no `vercel.json` queue trigger; no queue consumer; no workflow source; and no runtime ingress for `executeLunaWorkerStep`. The deployed `LunaDurableRuntime` was `UnavailableLunaDurableRuntime`, whose `dispatch()` returned `accepted: false` and `runId: null`. This made `WAITING_FOR_RUNTIME` factual and fail-closed.

## Sources

[1] [Vercel Queues: JS SDK Reference](https://vercel.com/docs/queues/sdk)

[2] [Vercel Queues concepts](https://vercel.com/docs/queues/concepts)

[3] [Vercel Workflows](https://vercel.com/docs/workflows)

[4] [Vercel Cron Jobs](https://vercel.com/docs/cron-jobs)

## Direct dashboard observation

On 2026-08-27, the existing `senota-ai` project Workflows page displayed **Get Started with Workflows**, not workflow runs, deployments, or a configured workflow. The page explicitly stated that the Workflow SDK can be added to Vite or Express applications with `npm i workflow`. This is evidence that the project is eligible to begin an integration, not evidence that a Workflow SDK integration, durable run, or configured consumer already exists.

## Official Workflow SDK integration requirement

The official [Workflow SDK Express guide](https://workflow-sdk.dev/docs/getting-started/express) requires `workflow`, `nitro`, and `rollup`; a `nitro.config.ts` using the `workflow/nitro` module; and changing the app build/development path to Nitro. The official [Vite guide](https://workflow-sdk.dev/docs/getting-started/vite) likewise requires `workflow`, `nitro`, the `workflow/vite` and `nitro/vite` plugins, and Nitro API-route handling. In both guides, a workflow uses the `"use workflow"` directive and durable work runs through `start()` from `workflow/api`.

The existing `senota-ai` build is not this shape: it uses Vite to produce the client plus esbuild to bundle `server/_core/index.ts` and `server/vercel-entry.ts` as a normal Express server. Therefore the documented Workflow SDK path is a deliberate build/entrypoint migration, not a drop-in package install.

## Queue integration staged for production validation

The selected Queue path was implemented without replacing Vite or Express: `@vercel/queue` was added; `VercelQueueLunaDurableRuntime` publishes only a bounded `luna_worker_v1` message with an idempotency key; `api/luna/queue-consumer.ts` is declared in `vercel.json` as a private `queue/v2beta` consumer; and the consumer maps provider delivery metadata to existing owner-scoped mission, task, worker, recovery, attention, version, and immutable audit records. It accepts a runtime only when `send()` returns the provider-issued `messageId`; a null identifier, local process, or dispatch failure remains `UNAVAILABLE` or `DEGRADED` with no fabricated run.

The consumer executes no shell command and no external web research. It can currently use only the reviewed, bounded Knowledge Space retrieval and in-process read-only scientific registry adapters. It checks persisted pause/cancel state before a worker step, respects task dependencies, records provider message IDs in immutable activity, uses bounded retry handling, and treats at-least-once redelivery as idempotent. This is **staged source**, not a production-success claim: deployment must still build the private function and the first accepted queue message must return a real provider `messageId` before the console can report a configured durable run.

## First production Queue deployment outcome

Deployment `dpl_5pK46wiQb6xqooRRU12r2w5NZ4Ae` for commit `90ca828` reached `READY` and reported two Node.js functions, confirming that Vercel built the added `api/luna/queue-consumer.ts` function. However, live production verification immediately found that existing `/api` owner-session and tRPC requests were intercepted by the new `api/` function namespace: the Knowledge Space unlock page reported that the administrator password was not configured and its API fetch attempted to parse the application shell as JSON. The runtime must therefore remain unaccepted; no controlled Luna mission was dispatched.

This is a real integration error, not a credential issue. Vercel’s Node documentation states that functions in `/api` are independently routed, while the existing application is a captured root `server.js` Express function. The next revision must explicitly preserve the Express API request surface while isolating the private Queue consumer. The failed release is a Vercel rollback candidate, and no scientific/provider, Knowledge Space, or cognitive record was altered by this routing probe.

[5] [Vercel Node.js Runtime: captured servers and `/api` functions](https://vercel.com/docs/functions/runtimes/node-js)

[6] [Vercel Express deployment guidance](https://vercel.com/docs/frameworks/backend/express)

[7] [Vercel Rewrites](https://vercel.com/docs/routing/rewrites)

[8] [Vercel Queues public-beta announcement](https://vercel.com/changelog/vercel-queues-now-in-public-beta)

[9] [Vercel `vercel.json` functions configuration](https://vercel.com/docs/project-configuration/vercel-json)

References [5]–[9] were consulted on 2026-08-27.

## Function-layout routing finding

The official Node.js runtime documentation, consulted on 2026-08-27, distinguishes a captured root `server.js` Node/Express server from independently routed files under `/api`. The queue release placed the private consumer in `/api/luna/queue-consumer.ts`, which Vercel correctly built as a second function, but that directory-level function routing took precedence over the existing captured server’s `/api/*` surface. The observed live symptom was a request intended for the owner/session API returning the application HTML shell. Any correction must preserve the captured Express API route space and must not merely reclassify this observed failure as missing credentials.

[10] [Vercel Node.js Runtime: captured servers and independently routed `/api` functions](https://vercel.com/docs/functions/runtimes/node-js)

[11] [Vercel Functions API Reference: other frameworks use `/api`](https://vercel.com/docs/functions/functions-api-reference)

## Routing-repair attempt 1: rejected by live verification

Commit `454a641` added a file-system catch-all at `api/[...path].ts` intended to relay ordinary API traffic to the existing Express handler while preserving the exact private Queue callback. Local execution-level testing of that relay returned the owner-status response as JSON. The deployment `dpl_4gapYcDnkYmY1ceNB7Dc8MmjPayJ` built successfully with three Node.js functions; nevertheless, a direct protected-browser request to `https://senota-ai.vercel.app/api/knowledge/owner/status` returned Vercel `404 NOT_FOUND`, not the required JSON response. This demonstrates that the generic Express project configuration did not expose the file-system catch-all as assumed. It is not accepted as a routing repair, and no Luna mission was dispatched.

The next minimal correction will use an explicit, documented same-application rewrite from ordinary `/api/:path*` traffic to one stable `/api` Express relay function, preserving the original logical path through a query parameter. The exact private Queue function remains separately configured with the `queue/v2beta` trigger and must not be exposed through this public rewrite.

## Routing-repair attempt 2: diagnosed and corrected for bundling

The explicit-rewrite deployment `dpl_Th7Fr4BBc8kmwjTxxWpMZf2TAnNN` reached `READY` with three Node functions but the owner-status probe returned `500 FUNCTION_INVOCATION_FAILED`. The production runtime log identified the precise cause: `api/index.js` could not resolve `/var/task/server/vercel-entry`. This was a packaged-function import failure, not an owner-password, Supabase, scientific-data, or Queue-provider failure.

The corrective revision changes no cognitive schema, knowledge record, provider record, or scientific boundary. It makes the existing build emit self-contained `api/index.js` and `api/luna/queue-consumer.js` artifacts from server-owned sources, updates the Queue trigger to the generated private artifact, and leaves the public API rewrite excluding that private Queue callback. Local focused routing and Luna tests passed before this deployment attempt; durable Queue execution is still not accepted until the repaired deployment returns a live provider message identifier and callback evidence.
